import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Useful when opening dev server from phone / another machine in LAN.
  // Add your local dev origins here if you see the allowedDevOrigins warning.
  // allowedDevOrigins: ["http://localhost:3000"],
};

export default nextConfig;
